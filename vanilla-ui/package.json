{
  "name": "dj-usb-tkit-vanilla-ui",
  "private": true,
  "version": "0.1.13",
  "license": "MIT",
  "engines": {
    "node": ">=24 <27"
  },
  "scripts": {
    "test:message-guard": "node scripts/check-message-handling.mjs",
    "build": "node scripts/build.mjs",
    "test:unit": "npm run test:message-guard && node --test tests/*.test.mjs",
    "test:coverage": "npm run test:message-guard && node --test --experimental-test-coverage tests/*.test.mjs",
    "test:e2e:nobuild": "playwright test",
    "test:e2e": "npm run build && npm run test:e2e:nobuild",
    "test:ci": "npm run build && npm run test:unit && npm run test:e2e:nobuild",
    "test": "npm run test:unit && npm run test:e2e",
    "test:e2e:coverage": "rm -rf .coverage-e2e && COVERAGE=1 node scripts/build.mjs && COVERAGE=1 playwright test && node scripts/report-e2e-coverage.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "v8-to-istanbul": "^9.3.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
